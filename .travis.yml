language:
  - python
python:
  - 3.5
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq stockfish
script:
  - python test_pystockfish.py
notifications:
  email:
    - zhelyabuzhsky@gmail.com
